tasks.register('publish01Bukkit', GradleBuild) {
    group = 'voicechat'
    tasks = [
            ':bukkit:clean',
            ':bukkit:curseforge',
            ':bukkit:modrinth',
            ':bukkit:publishAllPublicationsToHangar'
    ]
}

tasks.register('publish03Forge', GradleBuild) {
    group = 'voicechat'
    tasks = [
            ':common:clean',
            ':forge:clean',
            ':forge:curseforge',
            ':forge:modrinth',
            ':forge:modUpdate'
    ]
}

tasks.register('publish06API', GradleBuild) {
    group = 'voicechat'
    tasks = [
            ':api:clean',
            ':api:publish'
    ]
}

tasks.register('publish07APILocal', GradleBuild) {
    group = 'voicechat'
    tasks = [
            ':api:clean',
            ':api:publishToMavenLocal'
    ]
}

tasks.register('runClientForge', GradleBuild) {
    group = 'voicechat'
    tasks = [
            ':forge:runClient'
    ]
}

tasks.register('runServerForge', GradleBuild) {
    group = 'voicechat'
    tasks = [
            ':forge:runServer'
    ]
}

tasks.register('cleanAll', GradleBuild) {
    group = 'voicechat'
    tasks = [
            ':common:clean',
            ':api:clean',
            ':forge:clean',
            ':bukkit:clean'
    ]
}