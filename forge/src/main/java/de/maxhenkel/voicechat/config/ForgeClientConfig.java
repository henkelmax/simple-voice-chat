package de.maxhenkel.voicechat.config;

import de.maxhenkel.configbuilder.ConfigBuilder;

public class ForgeClientConfig extends ClientConfig {

    public ForgeClientConfig(ConfigBuilder builder) {
        super(builder);
    }
}
