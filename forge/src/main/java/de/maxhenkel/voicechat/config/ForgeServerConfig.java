package de.maxhenkel.voicechat.config;

import de.maxhenkel.configbuilder.ConfigBuilder;

public class ForgeServerConfig extends ServerConfig {

    public ForgeServerConfig(ConfigBuilder builder) {
        super(builder);
    }
}
